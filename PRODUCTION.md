# Production URLs (for reference only — not used by code)
#
# Default entry (root → app, via 301):
#   https://lodge.weichao.studio/
#
# App:
#   https://lodge.weichao.studio/dashboard.html
#
# Marketing / about page:
#   https://lodge.weichao.studio/about.html
#
# Custom domain configured in Vercel dashboard.
# The repo's config.json is excluded from Vercel via .vercelignore,
# so the deployed app shows a file picker on first run per device.
