# OS
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# Editors
.vscode/
.idea/
*.iml

# Node (if user uses npx serve)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.env

# Real user data — NEVER commit
config.json
config.real.json
config.local.json
config.backup*.json
config.json.bak-*
config.json.*.bak
*.bak
my-config.json
private-config.json

# Claude Code 本地工作笔记（含真实部署细节，不进公开仓库）
CLAUDE.md

# Local server logs
server.log
*.log

# Test artifacts (Playwright screenshots / cache)
.playwright-mcp/
lodge-*.png
scripts/.last-failure.png

# Temporary downloads (config-TIMESTAMP.json)
# These are intentionally allowed to be committed
# since they're harmless demo data with empty vault.
